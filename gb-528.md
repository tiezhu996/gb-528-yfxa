# gb-528 舞台吊挂 Cue 联锁

请生成英文短名为 `stage-rigging-cue-interlock` 的 Go 全栈 Web 项目。系统面向剧场技术团队，在离线排练环境中维护吊挂设备模型、Cue 动作和联锁规则，执行时序与碰撞风险推演并形成复核记录。系统不连接吊机、控制台、PLC 或舞台机械，不下发动作命令；任何结果仅供排练规划和人工安全审核。项目不做演出票务、场地预约、工单、库存、订单或财务。

## 项目主要需求

### 1. 核心实体

- `RiggingDevice`：字段至少含 `id`、`device_code`、`name`、`device_type`、`max_load_kg`、`max_speed_ms`、`travel_min_m`、`travel_max_m`、`safety_zone`、`device_status`、`version`；贯穿全栈。
- `CueDefinition`：字段至少含 `id`、`cue_code`、`name`、`sequence_no`、`start_offset_ms`、`duration_ms`、`cue_status`、`version`、`created_by`、`approved_by`；一个 Cue 关联多个动作，动作快照可用 JSONB，贯穿全栈。
- `InterlockRule`：字段至少含 `id`、`rule_code`、`rule_type`、`device_ids_json`、`threshold_json`、`severity`、`enabled`、`rule_version`、`explanation`；表达负载、速度、行程、空间和依赖规则，贯穿全栈。
- `RehearsalRun`：字段至少含 `id`、`cue_set_version`、`run_status`、`timeline_snapshot_json`、`rule_results_json`、`collision_windows_json`、`highest_severity`、`started_by`、`reviewed_by`、`finished_at`；离线结果不可覆盖，贯穿全栈。

### 2. 核心页面

- `/devices` 吊挂设备模型页：消费 `RiggingDevice`、`InterlockRule`，维护限制并查看适用规则。
- `/cues` Cue 编排页：消费 `CueDefinition`、`RiggingDevice`，编辑时序和动作参数并校验边界。
- `/rules` 联锁规则页：消费 `InterlockRule`、`RiggingDevice`，启停规则、测试阈值和查看影响范围。
- `/rehearsals` 离线推演页：消费 `RehearsalRun`、`CueDefinition`、`InterlockRule`，运行时间线并展示命中证据。
- `/audit` 审计复核页：消费 `RehearsalRun`、`CueDefinition`，比较版本、提交/批准并查看审计事件。

### 3. 差异算法与状态流

- 校验 Cue 依赖图并做拓扑排序，检测循环依赖、缺失前置和重复序号；循环时返回完整证据路径。
- 将设备动作展开为时间区间，计算位置线性插值，检测同安全区动作重叠、行程越界、速度超限、负载超限和互斥规则冲突。
- 每个联锁结果输出规则编号、Cue、设备、时间窗口、实际值、阈值和严重级别；相同快照必须得到相同结果，不得随机或硬编码。
- `CueDefinition` 状态为 `draft -> pending_review -> approved -> locked -> archived`；仅已批准且锁定版本可推演，出现 blocker 结果不得标记“可执行”，所有批准需要 reviewer 和事务/乐观锁。

### 4. 横切关注点

- JWT/RBAC 角色 `programmer`、`safety_reviewer`、`admin`，贯穿角色表、中间件、路由、前端守卫/按钮和类型。
- 审计记录设备限制、Cue 动作、规则版本、推演快照、状态与复核，含 request ID 和前后摘要。
- 全局错误处理统一图循环、动作越界、状态冲突、版本冲突和无权限；前端显式显示错误证据。
- 请求追踪、结构化日志、panic recovery、本地限流独立实现；日志不能包含 JWT 或完整敏感备注。

### 5. 共享枚举、组件、hooks 与中间件

- `CueStatus = draft | pending_review | approved | locked | archived`：后端 `backend/internal/constants/cue.go`，前端 `frontend/src/types/cue.ts`。
- `InterlockResult = pass | warning | blocker | invalid`：后端 `backend/internal/constants/interlock.go`，前端 `frontend/src/types/interlock.ts`。
- README 列明枚举在数据库、后端各层和前端 store/component/page 的全部出现位置。
- 共享组件：`CueStatusBadge`（Cue/审计页）、`TimelineTrack`（Cue/推演页）、`RuleEvidenceTable`（规则/推演页）。
- hooks：`useAuth`、`useRehearsalRun`，统一权限和推演轮询。
- 中间件至少独立实现 `RequestID`、`AccessLog`、`Auth`、`RBAC`、`Recovery`、`RateLimit`。

### 6. 技术与规模红线

- Vue 3 + TypeScript + Vite + Element Plus + Pinia，时间线和证据视图使用真实 API 数据。
- Go 1.22 + Gin + GORM + PostgreSQL；validator/v10、结构化日志、构造器注入、事务、`%w`、优雅停机。
- 功能 Go 代码 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行，不计测试/vendor/生成代码/前端；禁止空壳和职责合并。
- `/api/v1`、真实 `/healthz`、`/readyz`、统一响应/分页/错误码；拓扑、时间区间、插值、联锁和状态机必须测试。
- 禁止 TODO、placeholder、静态假结果、设备通信代码、handler 直连数据库或非事务审批。

### 7. 文件结构强制清单

```text
./
├── go.work                 # use ./backend
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/{config,model,dto,repository,service,handler,router,middleware,constants,interlock,util}/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/{api,stores,types,components/common,hooks,pages,router,utils}/
└── output/execution.md
```

实体逐层分文件；依赖图、时间线、位置插值、规则求值各自分文件。严禁合并职责到单一文件。

根目录 `runtime_smoke.json` 只能是可解析启动 manifest，构建、接口、Browser 和截图证据只能写入 `output/execution.md`。按下列内容创建，并支持 smoke SQLite 与 Compose PostgreSQL：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20528",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb528-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20528/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- Compose 名 `stage-rigging-cue-interlock`；前端 `18528`、后端 `19528`、PostgreSQL `57528`，容器内 `80/8080/5432`。
- Compose 顶层 `name`、无 `version:`，容器名带 `${COMPOSE_PROJECT_NAME:-stage-rigging-cue-interlock}`；数据库使用命名卷，数据库、后端和前端均配置 healthcheck 并按健康状态依赖，`docker compose ps` 中所有长驻服务最终必须为 healthy。
- `.env`/示例含 Compose 名、端口、数据库、JWT、CORS、推演步长和日志级别。Nginx `/api` 反代并支持 SPA；中文目录一键启动。
- README 中文且 Docker 启动优先，含复制 env、地址、测试账号、功能、技术栈、目录、变量、API、枚举位置、算法假设、醒目安全边界、开发命令、排错和 License。

### 9. 强制真实验收

1. 根目录执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`；规模实测必须为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；前端完成依赖安装、类型检查和 `npm run build` 生产构建。
2. 执行 Compose config、构建启动、ps、健康/就绪检查，确认数据库/后端 healthy、前端可访问。
3. 至少 8 项接口冒烟：登录、设备创建、Cue 创建、规则创建、循环依赖拒绝、推演运行、联锁证据查询、提交复核、锁定版本、越级拒绝和审计查询；真实结果只写入 `output/execution.md`，不得写入 `runtime_smoke.json`。
4. 只用 Codex 内置 Browser 验证登录、5 页面、Cue 时间线、规则编辑、推演证据和复核权限；禁止外部 Chrome，截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
5. 执行 `docker compose down -v --remove-orphans` 并确认本项目无容器残留，不影响其他项目。
6. 项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，将 hash 写入 `output/execution.md`，再把 `output/execution.md` 与截图作为第二次报告提交；最终 `git status --short` 为空。
